## 1.32.0

- 为数组函数结果、transformational intrinsic、数组构造器和 elemental 数组值建立统一的拥有型临时量语义目录。
- 通过过程级数据流计算临时量的创建、释放及逐控制流节点存活状态，拒绝逃逸和不完整的生命周期。
- 为分支、循环、返回、交替返回及 I/O `ERR/END/EOR` 边附加逐临时量的所有权证明。
- 以结构化清理动作替换数组清理字符串模板，按创建逆序释放，并依据语义元数据执行派生值终结。
- 让代码生成仅消费已验证的临时量计划，并拒绝缺失、不匹配或重复的清理动作。
- 确保无格式数组 I/O 在异常控制转移前释放语句拥有的临时值。
- 使用私有 lowering 副本生成普通调用和表达式调用，避免修改已验证的 typed IR。

## 1.31.0

- 增加通用反向不动点数据流求解能力，正确处理分支合流、循环闭环和不可达控制流。
- 为普通变量建立逐控制流节点的 use/def/live-in/live-out 分析，覆盖赋值、部分 designator、过程 `INTENT`、I/O、分配语句、DO latch 和嵌套动作。
- 将作用域清理计划与变量存活性证明绑定，代码生成会拒绝待清理对象仍在目标节点存活或缺少分析证明的控制流。
- 明确 BLOCK 每次重入产生的新对象实例，避免 `CYCLE`、`GOTO` 等转移让前一次实例错误存活或重复终结。
- 将表达式临时量、连续实参 bridge、host descriptor、求值顺序和 statement function 存储统一迁移到 typed IR 语义规划阶段，并加入资源溢出保护。
- 将函数结果 ABI、host capture 生命周期和 statement function 临时量归属语义层，进一步缩小代码生成阶段的职责。
- 代码生成现在只消费已验证的临时量规划，并由架构门禁阻止重新写入语义规划索引。
- 增加变量数据流、临时量生命周期、作用域清理和 BLOCK 重入的严格 C17、原生差分及 sanitizer 回归。

## 1.30.0

- 扩展过程级控制流图，以独立节点表示过程正常出口、image termination 和循环 latch，并区分 `RETURN`、`STOP`、交替返回及 I/O `END/EOR/ERR` 边。
- 为控制流节点建立双向前驱/后继连接和最大基本块，保证线性语句不会被错误拆分为单语句块。
- 修正单行 IF 中条件 `CYCLE/EXIT` 的真假路径，并为共享终止标签的嵌套旧式 DO 建立逐层 latch 链。
- 显式建模未处理 I/O 错误终止，同时避免在 `STOP/ERROR STOP` 和其他 image termination 路径上错误执行普通作用域终结。
- 增加可复用、有界并带边过滤的 bitset 工作队列数据流求解器，支持循环不动点、分支合流和纯标志状态。
- 将赋值标签和已赋值 FORMAT 的到达定义分析迁移到统一数据流基础设施，并保持多标签变量的可行边迭代收敛。
- 将每个作用域清理计划绑定到已验证的 CFG 源节点和目标节点，代码生成会拒绝未经控制流证明的清理计划。
- 增加控制流图、基本块、数据流边界以及 BLOCK 跨作用域终结的严格 C17、ASan/UBSan 和原生 Fortran 差分回归。

## 1.29.0

- 建立过程级控制流图，显式表示顺序、标签分支、循环回边/退出和 I/O 异常边，并计算语句可达性。
- 以不动点到达定义分析替代全单元 `ASSIGN` 扫描，覆盖条件分支、循环回边、普通整数重定义和可定义输出上下文。
- 在多个标签变量之间迭代收敛赋值 `GOTO` 的临时候选边，避免仅通过不可能目标到达的定义污染后续控制流。
- 强制检查默认 INTEGER 标签变量、有效可执行/FORMAT 目标、允许标签列表、结构化构造入口、全路径初始化及标签值的非法普通引用。
- 将同一到达标签分析用于已赋值 FORMAT，并把位置式 `READ`、`WRITE` 和 `PRINT` 控制项规范化为明确的 typed IR 角色。
- 代码生成仅消费语义阶段解析的赋值 `GOTO` 与 FORMAT 目标，去除重复 switch 标签，并直接使用逐目标作用域清理计划。
- 将控制流构建和赋值标签验证拆分为独立语义模块，并增加严格 C17、Sanitizer 和原生 Fortran 差分流水线覆盖。

## 1.28.0

- 统一标量控制语句中的数组值表达式准备流程，保持 typed shape、单次求值和所有权清理语义。
- 支持显式形状、可分配、指针、`CHARACTER` 及嵌套变换数组结果用于块/单行 `IF`、短路 `ELSE IF` 和算术 IF 选择器。
- 保证含临时数组的 `DO WHILE` 条件每轮重新求值，并在计数 DO 进入循环前各仅求值一次起始、终止和步长表达式。
- 支持数组相关的 `SELECT CASE`、计算 `GOTO`、交替 `RETURN` 和 `STOP` 选择器，并在分支或终止转移前释放临时值。
- 保持非拥有型指针结果存储不被误释放，并在标量值消费边界准确清理拥有型派生类型或字符结果。
- 修正导入数组函数的 rank/shape 不变量，使嵌套 `RESHAPE` 等变换表达式保留正确的 typed 结果维度。
- 将语句表达式物化与生命周期规划拆分为独立的代码生成模块。
- 增加控制表达式生命周期的严格 C17、ASan/UBSan、短路、副作用计数及原生 Fortran 差分验证。

## 1.27.0

- 统一显式形状、可分配和指针数组函数结果的描述符 ABI，完整携带类型、kind、rank、shape、字符长度、stride 与释放来源。
- 支持数组函数结果用于数组赋值、嵌套 elemental/归约表达式、普通函数实参及直接 `CALL` 实参，并保证函数引用只求值一次。
- 支持非连续及负步长指针函数结果的安全按值复制，不会错误释放非拥有型目标存储。
- 完善 CHARACTER 与含可分配组件的派生类型数组结果所有权，覆盖深复制、旧目标销毁、终结和临时量释放。
- 完善可分配数组函数结果的直接所有权转移、重分配下界语义、描述符完整性及溢出检查。
- 扩展显式接口兼容检查，验证函数结果的 type、kind、shape、字符长度及 `ALLOCATABLE/POINTER` 契约。
- 修正模块数组声明初始化器，生成符合严格 C17 的静态花括号初始化形式。
- 保持 `TRANSFER` 的 MOLD 为仅提供类型与形状且不求值的实参，避免在严格 C17 中生成未使用的数组构造器临时量。
- 增加数组函数结果的独立语义、严格 C17、Sanitizer 与原生 Fortran 差分验证。

## 1.26.0

- 为 `RESHAPE`、`PACK`、`UNPACK`、`SPREAD`、`CSHIFT`、`EOSHIFT`、`FINDLOC`、`TRANSPOSE` 和 `MATMUL` 建立统一的 typed intrinsic 身份与 shape 分派。
- 支持变换结果嵌套在其他变换、elemental 数组表达式、标量归约和假定形状过程实参中。
- 统一动态数组临时量的逐维 extent、CHARACTER 元素长度及派生类型深拷贝所有权，并保证控制表达式只求值一次。
- 完善九类变换 intrinsic 的关键字绑定及 type/kind/rank/shape 语义诊断，在生成 C 前拒绝负 extent、非法 `DIM`、不合形实参和错误 `ORDER`。
- 正确处理零大小嵌套结果，避免生成 C 中的静态除零诊断，并修正数组声明初始化器的严格 C17 形式。
- 增加数值、逻辑、字符、动态零大小及嵌套归约的原生 Fortran 差分测试。
- 增加含可分配组件派生类型的嵌套变换所有权测试，并在 ASan/UBSan 下验证深拷贝、终结和释放。
- 按职责拆分变换结果分配、元素复制、shape 提交和销毁逻辑，降低核心 emitter 的复杂度。

## 1.25.0

- 为 `ALL`、`ANY`、`COUNT`、`DOT_PRODUCT`、`MAXLOC`、`MAXVAL`、`MINLOC`、`MINVAL`、`PRODUCT` 和 `SUM` 建立统一的 typed IR、参数关联及 type/kind/rank/shape 契约。
- 完善数值、逻辑和复数归约的 `DIM` 与标量/数组 `MASK` 语义，支持动态维度、任意已支持 rank 及可分配数组结果。
- 完善 `MAXLOC` 与 `MINLOC` 的 `BACK`、`KIND`、逐维结果和全局坐标向量语义。
- 支持混合数值 kind、复数左实参共轭及不同 LOGICAL kind 的 `DOT_PRODUCT`。
- 正确处理非连续及反向数组段、零大小维度、空集单位元和零长度数组视图。
- 将数组值归约纳入统一临时量流程，使其可嵌套在 elemental 表达式及另一归约中，并保留动态 shape 和单次求值语义。
- 修正声明关键字作为变量名时的语句分类，同时保留不带 `::` 的初始化类型声明。

## 1.24.0

- 完善复数初始化常量求值，覆盖 kind=4/8、参数引用、混合数值算术、缩放除法、幂运算及全部已支持的复数数学 intrinsic。
- 支持 `CMPLX`、`CONJG`、`AIMAG`、`REAL`、`DBLE`、`INT` 和 `ABS` 在复数常量表达式中组合，并为模块、声明、`DATA` 与 `COMMON` 生成可移植的静态 C17 初始化器。
- 在生成代码前拒绝复数除零和复数零 `LOG`，避免在文件作用域产生运行时复数库调用。
- 为 `LOGICAL(L, KIND)` 增加 typed IR、kind 校验、常量折叠、elemental 数组语义和规范化 C17 转换。
- 支持 `LGE`、`LGT`、`LLE` 和 `LLT` 的关键字参数、空白补齐词法比较、常量折叠及 elemental 降级。
- 完善 `MAX0`、`MIN0`、`AMAX0`、`AMIN0`、`MAX1`、`MIN1`、`AMAX1`、`AMIN1`、`DMAX1` 和 `DMIN1` 的精确参数与结果类型契约。
- 修正旧式 extrema specific 的转换顺序，始终先按源类型选择结果，再执行受检的目标类型转换。
- 集中管理整数与浮点常量字面量生成，保证静态初始化器跨编译器一致，并扩展标量数学和转换表达式的静态折叠。
- 扩展严格 C17、sanitizer 和原生 Fortran 差分覆盖，并将字符 intrinsic 差分纳入默认 CTest 门禁。

## 1.23.0

- 为核心数学和数值转换 intrinsic 增加独立 typed IR、规范关键字关联、type/kind/rank 校验及 elemental 降级。
- 完善实数与复数 `ABS`、`ACOS`、`ASIN`、`ATAN`、`ATAN2`、`COS`、`COSH`、`EXP`、`LOG`、`LOG10`、`SIN`、`SINH`、`SQRT`、`TAN` 和 `TANH`，以及 `DPROD`、`MAX`、`MIN` 的可移植 C17/libm 降级。
- 支持保持 kind 的 `AIMAG`、`CMPLX`、`CONJG`、`DBLE`、`INT` 和 `REAL` 转换，覆盖标准旧式具体名称及乱序关键字参数。
- 将标量数学和转换表达式纳入溢出安全的常量求值器，支持目标 kind 舍入、定义域检查及整数范围保护。
- 保证 `MAX` 和 `MIN` 保留窄位及宽位整数 kind，并修正 `CMPLX` 默认 kind 和 `REAL(COMPLEX)` 分量 kind 语义。
- 在声明与初始化常量求值中统一识别 `ISO_FORTRAN_ENV` 整数和实数 kind 常量，包括 intrinsic `KIND=` 使用的局部参数别名。
- 保证显式声明的外部过程在语义校验、功能选择和 C17 生成中始终优先于同名 intrinsic。

## 1.22.0

- 支持内部过程捕获宿主局部 `ALLOCATABLE`、数据 `POINTER` 和 deferred-length `CHARACTER`，并双向同步存储地址、分配来源、字符长度及数组 lower、extent 和正负 stride。
- 完善内部子程序和函数的动态宿主状态传播，覆盖直接、兄弟、递归调用、提前 `RETURN`、释放与重分配，以及带 `SAVE` 的局部动态实体。
- 在宿主描述符回写和清理前安全保存标量、字符、派生类型及可分配函数结果，避免结果生命周期与宿主状态更新相互覆盖。
- 显式排序二元表达式、字符连接、用户过程及 intrinsic 实参中的副作用函数调用，同时保持数值模型 inquiry 和 `BIT_SIZE` 实参不被求值。
- 在宿主描述符桥接边界验证 rank、元素大小、整数范围、extent 和 stride，拒绝损坏或不兼容的动态状态。
- 按捕获解析与描述符生命周期拆分宿主关联代码生成模块，收敛语义、临时量规划和调用降级之间的职责。
- 增加严格 C17、ASan/UBSan 与原生 Fortran 差分覆盖，并隔离原生编译的模块产物，避免并发构建污染测试结果。

## 1.21.0

- 为 `AINT`、`ANINT`、`CEILING`、`DIM`、`FLOOR`、`MERGE`、`MOD`、`MODULO`、`NINT` 和 `SIGN` 完善 typed IR、常量求值与可移植 C17 降级，覆盖受支持的旧式具体名称、关键字参数、kind 选择和 elemental 数组。
- 集中管理派生类型值的深复制所有权，覆盖构造器、赋值、`WHERE`、过程实参、描述符和表达式临时量。
- 实现项目级具名及空白 `COMMON`、`BLOCK DATA`、模块 `DATA` 和可组合的 `EQUIVALENCE` 存储，支持别名初始化、零大小布局及可移植的非对齐访问。
- 支持 `RANDOM_NUMBER`、`RANDOM_SEED`、`DATE_AND_TIME`、`SYSTEM_CLOCK` 和 `CPU_TIME` intrinsic 过程，提供明确的实参契约和仅依赖 libc 的 C17 降级。
- 支持 alternate return 的类型化接口、调用、选择器、控制流分派及作用域清理。
- 扩展数据指针语义，支持仿射数组段、动态下界/extent/stride、跨过程关联、分配来源、rank remapping、`CONTIGUOUS` 和 `ASSOCIATED` 契约。
- 为派生类型数组指针组件增加完整的描述符路径，覆盖切片、赋值快照、过程调用、数组查询和分配生命周期。
- 增加显式的假定大小数组约束与安全查询降级，并通过统一数组表达式路径逐元素处理数组值查询结果。
- 强制检查动态数组段合形，并以宽位有符号下标运算保证边界、步长、向量下标和标量广播只求值一次。
- 通过显式类型化捕获支持单层内部过程宿主关联，覆盖直接、同级和递归调用，并对不支持的逃逸闭包及动态捕获给出硬诊断。

## 1.20.0

- 完整支持 `EXPONENT`、`FRACTION`、`NEAREST`、`RRSPACING`、`SCALE`、`SET_EXPONENT` 和 `SPACING` 实数表示 intrinsic。
- 完善 binary32 与 binary64 的参数关联、结果类型、kind、elemental rank/shape 和乱序关键字语义。
- 覆盖零、signed zero、subnormal、非有限值、极端整数指数等边界，并在生成代码前拒绝零方向的 `NEAREST`。
- 将实数表示 intrinsic 纳入溢出安全的常量求值器，为静态初始化生成精确十六进制 C17 常量。
- 使用按需内联且仅依赖 libc/libm 的可移植 C17 辅助函数完成动态降级，不引入外部运行时。
- 修正与 intrinsic 同名的显式 `EXTERNAL` 过程解析，使用户过程始终优先于内建函数。
- 在非 MSVC 平台通过 CMake 传播 libm 链接契约，并明确初始化常量求值状态，完善静态库消费者、Linux、WebAssembly 和 MSVC 的构建可移植性。
- 统一生成规范化的 IEEE 十六进制实数常量，消除不同 C 标准库格式化行为对生成代码的影响。

## 1.19.0

- 建立集中的可移植数值模型，明确 `INTEGER(KIND=1/2/4/8)`、IEEE binary32 与 binary64 的 radix、digits、precision、range、指数边界和极值，并在生成代码中验证目标平台契约。
- 为 `DIGITS`、`EPSILON`、`HUGE`、`KIND`、`MAXEXPONENT`、`MINEXPONENT`、`PRECISION`、`RADIX`、`RANGE`、`TINY`、`SELECTED_INT_KIND` 和 `SELECTED_REAL_KIND` 完善 typed IR、参数关联、结果类型及语义诊断。
- 将数值模型查询和 kind 选择纳入溢出安全的常量求值器，覆盖支持的整数/实数/复数 kind、标准失败码、参数表达式及声明 kind 选择器。
- 使用精确的 C17 常量降低数值查询，保证 inquiry 实参不被求值，并修正非默认整数 kind 的 `HUGE`、`DIGITS` 和 `RANGE` 语义。
- 为动态 `SELECTED_INT_KIND` 和 `SELECTED_REAL_KIND` 生成按需内联辅助函数，支持乱序关键字、可选参数及实参一次求值，且不引入外部运行时。
- 增加正向、负向、边界、严格 C17、ASan/UBSan 和原生 Fortran 差分测试，并将数值模型差分纳入数值验证流水线。

## 1.18.0

- 为 `BIT_SIZE`、`BTEST`、`IAND`、`IBCLR`、`IBITS`、`IBSET`、`IEOR`、`IOR`、`ISHFT`、`ISHFTC`、`NOT` 和 `MVBITS` 建立完整的 typed IR、参数关联和结果类型流程。
- 完善 `INTEGER(KIND=1/2/4/8)`、关键字参数、elemental 数组、静态 shape 及位置、长度和移位范围诊断，并在生成 C 前拒绝无效调用。
- 增加位操作常量折叠，覆盖符号位、完整位宽、零长度、循环移位和边界移位，同时保持目标整数 kind 的有限位宽语义。
- 使用无符号固定位宽运算与 `memcpy` 位复制生成可移植 C17，避免有符号移位、移位量等于位宽和严格别名未定义行为，且只在实际使用时生成对应支持代码。
- 为 `MVBITS` 实现标量别名、标量广播和重叠数组段的写入前快照语义，并增加严格 C17、UBSan、原生 Fortran 逐项差分及 CI 回归。
- 为 `ACHAR`、`ADJUSTL`、`ADJUSTR`、`CHAR`、`IACHAR`、`ICHAR`、`INDEX`、`LEN`、`LEN_TRIM`、`REPEAT`、`SCAN`、`TRIM` 和 `VERIFY` 完善 typed IR 签名、关键字关联、结果 kind/长度传播及语义诊断。
- 增加保持原始字节的字符常量折叠，覆盖嵌套变换、连接、空串、反向搜索和嵌入零字节。
- 为标量与 elemental 字符 intrinsic 增加可移植 C17 降级，包含长度溢出检查、位置边界、可复用临时对象及按需支持代码。
- 增加字符 intrinsic 组合的严格 C17 执行、ASan/UBSan 和原生 Fortran 字节级差分验证。

## 1.17.0

- 为 `INTERFACE`、`END INTERFACE` 和 `[MODULE] PROCEDURE` 具体过程列表建立结构化语法 AST，并保留精确源码范围。
- 完善命名泛型候选集，支持普通过程与模块过程、精确重载选择、完整过程签名及无匹配、歧义和非法接口诊断。
- 支持定义一元/二元运算符及内在运算符扩展，按照 Fortran 优先级完成 typed IR 绑定和具体函数代码生成。
- 支持标量及 `ELEMENTAL` 数组定义赋值，并通过具体子程序签名生成符合现有描述符 ABI 的 C17 调用。
- 完善特殊泛型设计符的 `PUBLIC/PRIVATE`、`USE ONLY`、模块导入和重导出语义。
- 增加严格 C17、原生 Fortran 差分、sanitizer 和架构回归。

## 1.16.0

- 为 `USE` 语句建立结构化语法 AST，支持 nature、空 `ONLY`、实体重命名和泛型设计符，并保留精确源码范围。
- 统一 `USE` 语句判定，支持将 `use` 用作普通实体名，并为非法关联、重复本地名称和冲突别名提供精确诊断。
- 完善模块关联语义，支持变量、常量、外部过程完整签名和派生类型本地别名，并正确隐藏已重命名的远端名称。
- 增加项目内模块依赖图、稳定拓扑排序及循环依赖诊断，使语义分析和 C17 生成不再依赖源码中的模块顺序。
- 隔离模块接口体作用域，避免接口内的 `USE`、隐式规则和派生类型泄漏到宿主模块，并完善 `LA_CONSTANTS` 的完整导入与重命名。
- 增加结构化语法、模块顺序、接口作用域、生成 C17 和精确诊断回归。

## 1.15.0

- 为 `PROGRAM`、`SUBROUTINE`、`FUNCTION` 和 `MODULE` 头部建立结构化语法 AST，统一保存过程前缀、哑实参、函数结果及精确源码范围。
- 将 `PROCEDURE(interface)` 声明迁移到独立语法 AST 和语义降级流程，完善接口、属性、实体列表及续行错误的定位诊断。
- 统一程序单元终止语句的结构化解析，验证开闭类型与名称，并修复模块扫描误收内部过程终止语句的问题。
- 增加架构边界、非法语法和精确诊断回归。

## 1.14.0

- 将字符长度、数组边界、声明初始化、语句函数结果及内置模块常量迁移到 canonical token range 和类型化表达式 IR。
- 从生产前端与语义路径中删除遗留标识符、实参拆分、整数文本求值和表达式文本查询辅助函数。
- 增加架构门禁，禁止生产代码恢复源码文本表达式解析入口及已经删除的文本解析器。
- 在语义检查中保留精确的过程设计符与关键字关联范围，并提供稳定的结构化诊断码和完整源码起止位置。
- 明确 CHARACTER 初始化长度的控制流以满足严格 MSVC 分析，并扩充诊断回归。

## 1.13.0

- 将调用、分配、停止、返回、普通赋值和指针赋值语句迁移到统一 token、AST 和类型化 IR 流程，并保留精确源码范围。
- 拆分动作语句解析与赋值代码生成，移除核心语句路径中的源码文本扫描和语义重新识别。
- 在语义阶段为标签跳转、`CYCLE`、`EXIT` 及 I/O `ERR`、`END`、`EOR` 建立逐边作用域清理计划。
- 完善 CHARACTER 停止码、`NULL(MOLD=...)`、非法动作语句、STOP 类型、PURE 限制及交替返回诊断。
- 增加严格 C17、ASan/UBSan、BLOCK 终结和指针语义回归。

## 1.12.0

- 将计数、无控制、`WHILE` 和标号 `DO`，单行、块和算术 `IF`，`ASSIGN`，以及直接、计算和赋值 `GOTO` 迁移到统一 token、AST 和类型化 IR 流程。
- 移除标号 DO 的源码改写，新增单一/共享终止标签所有权、合法终止动作执行、语句标签规范化和精确源码范围。
- 新增程序单元语句标签图验证，覆盖重复、未定义和非可执行目标、非法进入构造、兄弟分支间跳转、赋值标签，以及 I/O `ERR`、`END` 和 `EOR` 边。
- 将循环代码生成拆分为独立 emitter，为标签包装的语句提供稳定编号，并正确处理循环变量终值和结构化 `CYCLE`、`EXIT` 目标。
- 增加非法语法与语义回归、严格 C17 执行、sanitizer 和旧式控制流逐字节原生差分。

## 1.11.0

- 新增完整的 `INQUIRE(IOLENGTH=)` token、AST、类型化 IR、语义检查和 C17 代码生成流程。
- 使用无文件系统计数流复用真实无格式传输规则，支持所有整数结果 kind，并可将结果直接用于相同输出列表的直接访问 `RECL`。
- 扩展无格式数组传输，支持任意 rank 具名数组、仿射数组段、向量下标、数组构造器及可元素化数组表达式。
- 完善无格式隐式 DO 的一次求值、零步长和整数边界处理，并避免复数及默认派生对象输出表达式被重复求值。
- 增加 `IOLENGTH` 正负向语义、严格 C17 执行、原生 Fortran 差分及 sanitizer 回归，并纳入数值验证流水线。
- 延长短时 DGER 和 DTRSM 性能测量窗口，在不放宽逐项 5% 门槛的前提下降低共享 CI 运行器噪声。

## 1.10.0

- 新增顺序/直接访问、格式化/非格式化文件的记录式传输层，支持经过溢出检查的 `REC`/`RECL` 定位和固定记录边界。
- 新增可移植的小端顺序非格式化记录协议、损坏记录检测、部分记录清理及记录感知的 `BACKSPACE`。
- 支持已实现标量 kind、复数、字符、固定数组、隐式 DO，以及派生对象的默认或定义 I/O 二进制传输。
- 统一 `IOSTAT`、`IOMSG`、`ERR`、`END` 和 `EOR` 处理，并在不生成中止占位代码的前提下检查文件单元的动作、格式、访问和记录约束。
- 修正直接格式化文件的空格填充、斜杠编辑、格式回转、缺失记录处理和跨定长记录的 `NEXTREC` 推进。
- 增加非法记录控制项，以及动态组件派生对象必须使用定义 I/O 的生成前语义检查。
- 在 CI 中增加严格 C17 执行、损坏文件回归、sanitizer 和生成 C/原生 Fortran 记录 I/O 差分。

## 1.9.0

- 新增统一、可定位的生成代码 I/O 流抽象，外部文件继续使用 libc，内部文件直接使用内存记录。
- 移除内部文件的 `tmpfile()` 模拟和回读复制，支持字符标量/数组、多记录、PAD、记录定位及溢出状态。
- 将显式格式、列表导向、NAMELIST 和定义 I/O 迁移到同一流接口，并保留外部文件的批量读写路径。
- 增加内部文件格式化约束检查，拒绝标准禁止的非前进控制项和无格式传输。
- 将列表导向与内存流生成支持从核心转译器中拆分为独立模块，进一步收敛代码生成职责。
- 增加原生 Fortran 语义差分、严格 C17、sanitizer 及无文件系统 WebAssembly 执行回归。

## 1.8.0

- 将常量、标签和已赋值 FORMAT 迁移到统一的 token、AST、类型化 IR 和静态 C17 指令流程。
- 为真正的运行时字符格式增加动态解析状态，移除嵌套、字面量、DT iotype 和 v-list 的固定容量限制。
- 完善嵌套组、无限组、定位、比例、符号、小数控制及定义 I/O 格式元数据的执行语义。
- 修正 `E`、`D` 和 `G` 描述符的尾数、指数、有效数字、极值及记录定位输出，使其与原生 Fortran 一致。
- 修复无效 FORMAT 描述符解析失败时的临时 AST 所有权清理，消除 sanitizer 检出的内存泄漏。
- 修复 FORMAT 中 `(/` 被统一词法流误判为数组构造定界符的问题，恢复官方 BLAS 标签格式。
- 增加 FORMAT 描述符逐字节原生差分、深层/超长资源边界、严格 C17 和 sanitizer 回归。

## 1.7.0

- 将 `PRINT` 迁移到统一 token、AST 和类型化 I/O 流程，移除文本切割与表达式二次解析。
- 统一 `PRINT`、`READ` 和 `WRITE` 的格式化传输引擎及格式语义诊断。
- 支持 `PRINT` 的列表导向、字符常量、运行时字符、标签格式、空输出列表和嵌套 implied-DO。
- 支持 F77 已赋值 FORMAT 变量，并生成动态、无固定容量的格式标签选择逻辑。
- 修正格式末尾定位描述符产生多余记录空格的问题，使输出记录与原生 Fortran 一致。
- 增加严格 C17、ASan/UBSan、原生 Fortran 输出差分及固定 Reference BLAS/LAPACK 回归。

## 1.6.0

- 将宏定义管理拆分为独立、有界的预处理模块，并支持源码内函数式宏。
- 增加零参数、嵌套和可变参数宏，以及字符串化、token 粘贴和宏重定义检查。
- 增加反斜杠续接的预处理指令逻辑行及宏展开生成的 include operand。
- 完善 `#if` 的 64 位有符号/无符号整数、字符常量、溢出和非法运算诊断。
- 增加宏参数资源限制，并扩充严格 C17、sanitizer 和固定 Reference BLAS/LAPACK 回归。

## 1.5.0

- 将外部文件控制语句迁移到统一 token、AST、类型化 IR 和结构化代码生成流程。
- 完善 `OPEN` 与 `CLOSE` 的文件状态、访问方式、动作、格式、位置、字符控制属性及文件处置语义。
- 新增顺序文件 `BACKSPACE` 与基于标准 C17 的物理 `ENDFILE` 截断实现。
- 新增按文件单元或文件名执行的 `INQUIRE`，返回连接状态、能力和文件属性。
- 统一文件控制语句的 `IOSTAT`、`IOMSG` 和 `ERR` 错误处理，并增加生成前语义检查。
- 增加严格 C17、sanitizer、执行回归、gfortran 差分和数值验证流水线覆盖。

## 1.4.0

- 将 `DATA` 语句完整迁移到统一 token、AST 和类型化 IR 流程，支持多组数据、重复因子和任意嵌套隐式 DO。
- 增加初始化常量、类型与 kind、元素数量、数组边界、重复初始化及资源限制的生成前语义检查。
- 修正标量、数组、字符、复数及 `EQUIVALENCE` 根存储的一次性初始化和 `SAVE` 生命周期语义。
- 将可移植的标量、整数组、部分数组及隐式 DO 初始化生成为高效、可读的 C17 静态初始化器。
- 扩充严格 C17、重复调用、负向诊断和固定 Reference BLAS/LAPACK 官方数值差分覆盖。

## 1.3.0

- 新增独立、有界的条件预处理器，支持动态条件嵌套、完整整数表达式求值和递归对象宏展开。
- 增加 `#include`、Fortran `INCLUDE`、`#line` 和数字行标记，并通过回调保持核心库不依赖文件系统。
- 增加 API 预处理定义以及 CLI `-D`、`-U`、`-I` 选项，明确项目级与源码局部宏作用域。
- 统一预处理、源码规范化与 token 的源码映射，结构化诊断可同时报告宏展开位置和定义位置。
- 增加预处理字节、宏数量与深度、include 数量与深度等请求级资源限制及负向测试。
- 扩充独立预处理、CLI include、跨编译器、sanitizer 和固定 Reference LAPACK 全量回归。
- 修复严格 LLVM/Linux 构建中预处理测试的 `SIZE_MAX` 声明可移植性问题。

## 1.2.0

- 统一并完善 token、AST、类型化 IR、语义分析和代码生成流程，进一步拆分核心模块职责。
- 完善数组 inquiry、数组表达式、数组构造器、向量下标及 transformational intrinsic 的类型和语义。
- 扩展 `TRANSPOSE`、`MATMUL` 等变换操作对动态数组、混合 kind、字符、复数和派生类型的支持。
- 改进假定形状数组描述符、非连续数组段、过程实参临时量以及字符和派生类型的所有权管理。
- 增加 C17、原生 Fortran 差分、sanitizer、语义负向测试和数值验证流水线覆盖。

## 1.1.0

- 重构前端、语义分析、AST、类型化 IR 和代码生成管线。
- 完善声明、隐式类型、存储关联、表达式与语句的解析和语义检查。
- 增加请求级资源限制、结构化诊断回调和严格的配置校验。
- 改进命令行流式输入输出与文件原子写入，避免失败时留下不完整产物。
- 扩充架构边界、命令行、语义分析及生成代码回归测试。

## 1.0.0

- 使用 C17 和 CMake 重构为现代化、模块化、跨平台的 f2c。
- 生成自包含 C17 代码，不再依赖额外的运行时库。
- 建立跨平台测试、sanitizer、模糊测试、可复现构建和发布验证体系。
- 建立完整的固定 BLAS/LAPACK 编译、正确性差分和性能门禁。
- 使用 MIT 许可证发布。
